# venv_wan — Wan2.2-I2V motion video (Alibaba/Tongyi Wanxiang). Python 3.12.
# torch ~2.7 (cu126) is installed separately by setup/make_wan_venv.sh.
# WanImageToVideoPipeline is recent, so pull diffusers from git main.
diffusers @ git+https://github.com/huggingface/diffusers.git
# Wan2.2's diffusers integration needs transformers 4.49-4.51.3, incompatible
# with every other venv's pins (see core/config.py's WAN_I2V_REPO comment),
# hence its own venv.
transformers>=4.49,<=4.51.3
accelerate
safetensors
sentencepiece   # Wan's UMT5 text encoder
imageio
imageio-ffmpeg
av
setuptools<81   # Python 3.12 venvs omit setuptools; some deps still need pkg_resources
