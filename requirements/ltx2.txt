# venv_ltx2 — LTX-2.3 (Lightricks) image+prompt -> motion video w/ audio.
# Python 3.12. torch ~2.7 (cu126) is installed separately by
# setup/make_ltx2_venv.sh.
#
# LTX-2.3's pipeline unconditionally imports Gemma3ForConditionalGeneration
# (its default text encoder) at module load time, needing transformers>=~4.50
# -- confirmed via a real ImportError in Colab when this briefly shared a
# venv with the now-removed LTX-Video-0.9.7-distilled (which needed
# transformers<4.50 for an unrelated tokenizer regression), not assumed.
# Install transformers from git here too, same reasoning as
# requirements/qwen.txt: guarantees current model support rather than
# guessing a version boundary.
diffusers @ git+https://github.com/huggingface/diffusers.git
transformers @ git+https://github.com/huggingface/transformers.git
accelerate
safetensors
sentencepiece
tiktoken
imageio
imageio-ffmpeg
av
setuptools<81
